<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dddinjava.portal.dao.AccountMapper">
    <insert id="save" parameterType="Account">
        insert into account (username, password, owner_full_name)
        values (#{username}, #{password}, #{ownerFullName})
    </insert>

    <select id="list" resultType="Account">
        select id, username, password, owner_full_name as ownerFullName from account
    </select>
</mapper>